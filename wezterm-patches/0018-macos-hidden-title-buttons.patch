diff --git a/wezterm-gui/src/termwindow/render/paint.rs b/wezterm-gui/src/termwindow/render/paint.rs
--- a/wezterm-gui/src/termwindow/render/paint.rs
+++ b/wezterm-gui/src/termwindow/render/paint.rs
@@ -253,6 +253,7 @@ impl crate::TermWindow {
         }
 
         self.update_window_drag_exclusion();
+        self.update_vtabs_title_buttons();
         for pos in panes {
             if pos.is_active {
                 self.update_text_cursor(&pos);
diff --git a/wezterm-gui/src/termwindow/ui_host.rs b/wezterm-gui/src/termwindow/ui_host.rs
--- a/wezterm-gui/src/termwindow/ui_host.rs
+++ b/wezterm-gui/src/termwindow/ui_host.rs
@@ -889,7 +889,7 @@ impl TermWindow {
             && self.vtabs_titlebar_height() > 0.
     }
     /// A left sidebar wide enough for the native buttons carries the header itself.
-    fn vtabs_inline_header(&self) -> Option<f32> {
+    fn vtabs_native_header_inset(&self) -> Option<f32> {
         vtabs_sidebar_header_inset(
             self.vtabs_integrated_macos(),
             self.vtabs_reserved_width(),
@@ -901,9 +901,28 @@ impl TermWindow {
             },
         )
     }
+    /// Without a sidebar, or beside a left one too narrow for them, the native buttons hide.
+    fn vtabs_title_buttons_hidden(&self) -> bool {
+        let narrow_left =
+            self.vtabs_left_width() > 0. && self.vtabs_native_header_inset().is_none();
+        self.ui_host.is_some()
+            && self.vtabs_integrated_macos()
+            && (self.vtabs_reserved_width() == 0. || narrow_left)
+    }
+    fn vtabs_inline_header(&self) -> Option<f32> {
+        self.vtabs_native_header_inset()
+            .or_else(|| self.vtabs_title_buttons_hidden().then_some(0.))
+    }
+    pub fn update_vtabs_title_buttons(&self) {
+        if let Some(window) = self.window.as_ref() {
+            window.set_title_buttons_hidden(self.vtabs_title_buttons_hidden());
+        }
+    }
     /// Content then starts at the frame gutter, matching its other three sides.
     pub fn vtabs_content_top(&self) -> f32 {
-        if self.vtabs_inline_header().is_some() && self.vtabs_left_width() > 0. {
+        if self.vtabs_title_buttons_hidden()
+            || (self.vtabs_inline_header().is_some() && self.vtabs_left_width() > 0.)
+        {
             0.
         } else {
             self.vtabs_titlebar_height()
@@ -946,7 +965,7 @@ impl TermWindow {
     }
     fn ensure_vtabs_titlebar(&mut self, ui: &mut UiHost, width: f32) -> anyhow::Result<()> {
         let height = self.vtabs_titlebar_height();
-        if height == 0. {
+        if height == 0. || width <= 0. {
             ui.chrome = None;
             ui.chrome_items.clear();
             return Ok(());
diff --git a/window/src/lib.rs b/window/src/lib.rs
--- a/window/src/lib.rs
+++ b/window/src/lib.rs
@@ -295,6 +295,9 @@ pub trait WindowOps {
     /// Content that a mouse down never drags, even under a transparent macOS titlebar
     fn set_window_drag_exclusion(&self, _content: Rect) {}
 
+    /// Hides the native macOS title buttons while keeping the window's decorations
+    fn set_title_buttons_hidden(&self, _hidden: bool) {}
+
     /// Changes the location of the window on the screen.
     /// The coordinates are of the top left pixel of the
     /// client area.
diff --git a/window/src/os/macos/window.rs b/window/src/os/macos/window.rs
--- a/window/src/os/macos/window.rs
+++ b/window/src/os/macos/window.rs
@@ -391,6 +391,7 @@ pub(crate) struct WindowInner {
     view: StrongPtr,
     window: StrongPtr,
     config: ConfigHandle,
+    title_buttons_hidden: bool,
 }
 
 fn function_key_to_keycode(function_key: char) -> KeyCode {
@@ -535,6 +536,7 @@ impl Window {
                 &window,
                 config.window_decorations,
                 config.integrated_title_button_style,
+                false,
             );
 
             // Prevent Cocoa native tabs from being used
@@ -659,6 +661,7 @@ impl Window {
                 window,
                 view,
                 config: config.clone(),
+                title_buttons_hidden: false,
             }));
             inner.borrow_mut().window.replace(weak_window);
             conn.windows
@@ -851,6 +854,13 @@ impl WindowOps for Window {
         });
     }
 
+    fn set_title_buttons_hidden(&self, hidden: bool) {
+        Connection::with_window_inner(self.id, move |inner| {
+            inner.set_title_buttons_hidden(hidden);
+            Ok(())
+        });
+    }
+
     fn get_clipboard(&self, _clipboard: Clipboard) -> Future<String> {
         Future::result(
             ClipboardContext::new()
@@ -1070,6 +1080,7 @@ impl WindowInner {
                 &self.window,
                 self.config.window_decorations,
                 self.config.integrated_title_button_style,
+                self.title_buttons_hidden,
             );
         }
     }
@@ -1123,6 +1134,7 @@ impl WindowInner {
                         &self.window,
                         self.config.window_decorations,
                         self.config.integrated_title_button_style,
+                        self.title_buttons_hidden,
                     );
                     self.window.setFrame_display_(saved_rect, YES);
                     self.window.makeKeyAndOrderFront_(nil);
@@ -1258,6 +1270,7 @@ impl WindowInner {
                 &self.window,
                 self.config.window_decorations,
                 self.config.integrated_title_button_style,
+                self.title_buttons_hidden,
             );
 
             self.update_titlebar_background();
@@ -1373,6 +1386,19 @@ impl WindowInner {
         }
     }
 
+    fn set_title_buttons_hidden(&mut self, hidden: bool) {
+        if self.title_buttons_hidden != hidden {
+            self.title_buttons_hidden = hidden;
+            let decorations = effective_decorations(
+                self.config.window_decorations,
+                self.config.integrated_title_button_style,
+            );
+            let decorated = decorations
+                .intersects(WindowDecorations::TITLE | WindowDecorations::INTEGRATED_BUTTONS);
+            hide_title_buttons(&self.window, hidden || !decorated);
+        }
+    }
+
     fn is_zoomed(&self) -> bool {
         unsafe { msg_send![*self.window, isZoomed] }
     }
@@ -1453,10 +1479,24 @@ fn effective_decorations(
     decorations
 }
 
+fn hide_title_buttons(window: &StrongPtr, hidden: bool) {
+    unsafe {
+        for titlebar_button in &[
+            appkit::NSWindowButton::NSWindowMiniaturizeButton,
+            appkit::NSWindowButton::NSWindowCloseButton,
+            appkit::NSWindowButton::NSWindowZoomButton,
+        ] {
+            let button = window.standardWindowButton_(*titlebar_button);
+            let _: () = msg_send![button, setHidden: to_yes_no(hidden)];
+        }
+    }
+}
+
 fn apply_decorations_to_window(
     window: &StrongPtr,
     decorations: WindowDecorations,
     integrated_title_button_style: IntegratedTitleButtonStyle,
+    title_buttons_hidden: bool,
 ) {
     let mask = decoration_to_mask(decorations, integrated_title_button_style);
     let decorations = effective_decorations(decorations, integrated_title_button_style);
@@ -1466,14 +1506,7 @@ fn apply_decorations_to_window(
         let has_decorations = decorations.contains(WindowDecorations::TITLE)
             || decorations.contains(WindowDecorations::INTEGRATED_BUTTONS);
 
-        for titlebar_button in &[
-            appkit::NSWindowButton::NSWindowMiniaturizeButton,
-            appkit::NSWindowButton::NSWindowCloseButton,
-            appkit::NSWindowButton::NSWindowZoomButton,
-        ] {
-            let button = window.standardWindowButton_(*titlebar_button);
-            let _: () = msg_send![button, setHidden: to_yes_no(!has_decorations)];
-        }
+        hide_title_buttons(window, !has_decorations || title_buttons_hidden);
 
         window.setTitleVisibility_(if decorations.contains(WindowDecorations::TITLE) {
             appkit::NSWindowTitleVisibility::NSWindowTitleVisible
