# Uncomment before v1
# name: Release
# on:
#   push:
#     tags:
#       - "v*"
# permissions:
#   contents: write
# defaults:
#   run:
#     working-directory: backend
# jobs:
#   build:
#     name: ${{ matrix.target }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - target: aarch64-apple-darwin
#             os: macos-14
#           - target: x86_64-apple-darwin
#             os: macos-14
#           - target: x86_64-unknown-linux-gnu
#             os: ubuntu-latest
#             cross: true
#           - target: aarch64-unknown-linux-gnu
#             os: ubuntu-latest
#             cross: true
#           - target: x86_64-pc-windows-msvc
#             os: windows-latest
#             ext: .exe
#     steps:
#       - uses: actions/checkout@v4
#       - uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: ${{ matrix.target }}
#       - uses: Swatinem/rust-cache@v2
#         with:
#           workspaces: backend
#           key: ${{ matrix.target }}
#       - name: Install cross
#         if: matrix.cross
#         run: cargo install cross --locked
#       - name: Build
#         shell: bash
#         run: |
#           if [ "${{ matrix.cross }}" = "true" ]; then
#             cross build --release --target ${{ matrix.target }}
#           else
#             cargo build --release --target ${{ matrix.target }}
#           fi
#       - name: Stage asset
#         shell: bash
#         run: |
#           mkdir -p dist
#           cp target/${{ matrix.target }}/release/wez-vtabs${{ matrix.ext }} dist/wez-vtabs-${{ matrix.target }}${{ matrix.ext }}
#       - uses: actions/upload-artifact@v4
#         with:
#           name: wez-vtabs-${{ matrix.target }}
#           path: backend/dist/*
#           if-no-files-found: error
#   release:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/download-artifact@v4
#         with:
#           path: dist
#           merge-multiple: true
#       - uses: softprops/action-gh-release@v2
#         with:
#           files: dist/*
